# Screenshots

Drop PNGs here so the root `README.md` renders them. Capture at ~1280px wide on the warm light theme:

| File | What to capture |
|------|-----------------|
| `today.png` | The **Today** dashboard — greeting, the conversion funnel (Hunted → … → Offer), and the "What's working" source panel with a few sent emails so the bars are populated. |
| `hunt.png` | **Hunt** results for a query like `react engineer remote` — contact cards showing the email-status / confidence badges. |
| `compose.png` | **Compose** — one expanded, generated draft so people can see the email quality. |

Tip: seed a handful of contacts and mark a couple `replied`/`interview`/`offer` first, so the funnel and analytics look alive rather than empty.
